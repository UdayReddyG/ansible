- name: checking the user
  hosts: web
  become: yes
  tasks:
  - name: getting user
    ansible.builtin.command: id expense
    register: USER

  - name: checking info
    ansible.builtin.debug:
     msg: "user info: {{USER}}"
